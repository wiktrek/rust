use std::io;
fn main() {
    let mut user_input = String::new();
    println!("");
    io::stdin()
    .read_line(&mut user_input).expect("Couldn’t read from stdin");
let a = "ee"; 
println!("{}", a);
}

