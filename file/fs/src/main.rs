use std::{ io, fs};
fn main() {
    let mut action: String = String::new(); 
    println!("Hello, world!");
io::stdin()
    .read_line(&mut action).expect("Couldn’t read from stdin");

    match action {
    }
}
